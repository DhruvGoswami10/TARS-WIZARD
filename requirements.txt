# TARS-WIZARD Python Dependencies
# Install with: pip install -r requirements.txt
#
# Note: Some packages are Pi-only (marked below).
# On PC/Mac, install with: pip install -r requirements.txt
# Pi-only packages will be skipped if hardware is unavailable.

# AI & Language Models
openai>=1.0.0

# Voice & Audio
edge-tts>=7.2.7
SpeechRecognition>=3.10.0
pydub>=0.25.1
PyAudio>=0.2.13

# Configuration
python-dotenv>=1.0.0
PyYAML>=6.0

# Terminal UI
rich>=13.0.0

# HTTP
requests>=2.31.0

# Linting & Testing (dev)
ruff>=0.4.0
pytest>=8.0.0
pytest-cov>=5.0.0

# ─── Pi-Only Dependencies ───────────────────────────────────
# These will fail on non-Pi systems. Install separately on Pi:
#   pip install adafruit-circuitpython-pca9685 adafruit-blinka evdev picamera2
#
# adafruit-circuitpython-pca9685>=3.4.0
# adafruit-blinka>=8.0.0
# evdev>=1.6.0
# picamera2>=0.3.0
# ultralytics>=8.0.0
