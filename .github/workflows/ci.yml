name: CI

# Disabled — legacy code moved to software/legacy/.
# Will be re-enabled when new software/ code is added.
on:
  workflow_dispatch:

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - run: echo "CI will be re-enabled with new codebase"
